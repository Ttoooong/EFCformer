# 智能热点收集助手（免费版）项目设计

## 目标与约束
- **目标**：用户输入关键词（支持多词/短语/主题），系统自动检索最新相关消息，聚合、去重、摘要、排序并输出。
- **约束**：**完全免费**，不使用任何需要付费的 API（包括有免费额度但绑定信用卡/可能收费的服务）。
- **优先级**：可用性 > 数据新鲜度 > 体验细节；先跑通核心流程，再逐步优化。

## 核心场景
1. 用户输入关键词（如“电动汽车 补贴”）。
2. 系统根据关键词到多来源检索（RSS/公开站点搜索页/开放数据源）。
3. 聚合、去重、过滤低质量条目。
4. 输出最新、相关度最高的结果（标题+来源+时间+摘要/片段+链接）。

## 数据源策略（免费）
> 只使用**公开免费**来源，避免任何商业 API。

### 1) RSS/Atom 订阅源（优先）
- 新闻站点、行业媒体、博客、政府网站等常见提供 RSS/Atom。
- 优点：结构化、稳定、免费。
- 缺点：覆盖面可能有限，需要维护源列表。

### 2) 站内搜索页抓取（无 API）
- 对不提供 RSS 的站点，抓取站内搜索结果页（需遵守 robots.txt）。
- 解析 HTML 列表，提取标题/摘要/时间/链接。
- 优点：覆盖更多站点。
- 缺点：页面结构变化、需要维护解析规则。

### 3) 公共数据/开放平台（免费且无需绑卡）
- 政府、机构、学校等开放数据/公告页面。
- 使用公开 HTML/RSS 页面或文件下载。

### 4) 用户自定义来源
- 用户可添加/删除 RSS 或页面抓取规则（降低维护成本）。

## 架构设计

### 模块划分
1. **采集层（Collector）**
   - RSS/Atom 采集器
   - HTML 抓取器（站内搜索/列表页）
   - 统一输出 `RawItem` 数据结构

2. **解析层（Parser）**
   - 统一字段：`title`, `url`, `source`, `published_at`, `summary`, `keywords`
   - 时间标准化、去噪、去 HTML

3. **过滤与去重（Filter & Dedup）**
   - 关键词相关性评分（基于词频/TF-IDF/简单语义匹配）
   - URL 规范化去重（移除 tracking 参数）
   - 标题+摘要相似度去重（Jaccard/SimHash）

4. **排序（Ranker）**
   - 时间衰减 + 相关性 + 来源权重

5. **输出层（Presenter）**
   - CLI 输出（MVP）
   - Web UI/接口（后续）

### 数据结构
```json
{
  "title": "...",
  "url": "...",
  "source": "...",
  "published_at": "2025-01-01T10:00:00Z",
  "summary": "...",
  "keywords": ["..."],
  "score": 0.85
}
```

## 关键功能设计

### 1) 关键词处理
- 分词：使用开源分词（如 `jieba`）
- 扩展：同义词扩展（可用开源词库）
- 过滤：停用词处理

### 2) 采集策略
- 定时抓取（如每 10-30 分钟一次）
- 短期缓存避免重复请求
- 站点级限速（避免被封）

### 3) 相关性评分（免费）
- 规则+统计混合：
  - 标题匹配权重最高
  - 摘要匹配次之
  - 时间越新分数越高

### 4) 去重策略
- URL 去参数去重
- 标题/摘要近似匹配

## 技术选型（完全免费）
- **语言**：Python
- **抓取**：`requests` + `beautifulsoup4` + `feedparser`
- **分词**：`jieba`
- **存储**：SQLite（本地免费）
- **调度**：`cron` 或 `APScheduler`
- **可视化**：简单 CLI/后续可用 `FastAPI` + 前端（不依赖付费服务）

## MVP 里程碑
1. **MVP-1（核心功能）**
   - 输入关键词
   - RSS 源检索
   - 输出最新结果

2. **MVP-2（质量提升）**
   - HTML 抓取
   - 去重与排序
   - 关键词相关性评分

3. **MVP-3（体验提升）**
   - Web UI
   - 用户自定义来源
   - 订阅/提醒

## 风险与对策
- **站点结构变动**：配置化解析规则 + 定期检查
- **内容版权**：仅展示标题/摘要 + 原文链接
- **性能问题**：缓存 + 限速 + 并发抓取

## 后续扩展
- 主题聚类
- 趋势分析（词频变化）
- 多语言支持

---

如需我进一步补充：
- 具体开源 RSS 源列表
- 详细数据库 Schema
- MVP 代码结构模板
请告诉我。
